import styled from "styled-components";
export const EmailWrapper = styled.div`
     position: relative;
     z-index: 5;
     margin: 1rem 0;
     & > button {
          width: 100%;
          justify-content: center;
          padding: 1rem;
          opacity: 1;
     }
`