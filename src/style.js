import styled, { keyframes } from 'styled-components';
const animationScale = keyframes`
  0% {
    transform: scale(0.7);
  }
  50% {
    transform: scale(0.85);
  }
  100% {
    transform: scale(1);
  }
`

